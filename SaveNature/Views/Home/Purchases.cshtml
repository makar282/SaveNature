<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>СОХРАНИ ПРИРОДУ | Покупки</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/purchases.css" />
</head>
<body>
    <div class="purchases-container">
        <div class="purchases-card">
        @if (User.Identity != null && User.Identity.IsAuthenticated)            {
                <!-- Секция для ввода чеков -->
                <div class="qr-section">
                    <h3>Добавить чек</h3>
                    <form id="qrCodeForm" class="qr-form">
                        <input type="hidden" id="userName" name="userName" value="@User.Identity.Name" />
                        <div class="mb-3">
                            <label for="qrRaw" class="form-label">QR-код (строка)</label>
                            <input type="text" class="form-control" id="qrRaw" name="qrRaw" placeholder="t=20200924T1837&s=349.93&fn=9282440300395&i=46534&fp=1273019065&n=1">
                        </div>
                        <div class="mb-3">
                            <label for="qrUrl" class="form-label">URL картинки QR-кода</label>
                            <input type="text" class="form-control" id="qrUrl" name="qrUrl" placeholder="https://example.com/qrimage.jpg">
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success">Отправить</button>
                            <button type="button" class="btn btn-primary" id="scanQrCode"><i class="bi bi-qr-code-scan"></i> Сканировать QR</button>
                            <button type="button" class="btn btn-info" id="displayPurchases">Отобразить покупки</button>
                        </div>
                    </form>
                    <div id="formMessage" class="mt-3"></div>
                </div>

                <!-- Секция для таблицы покупок -->
                <div class="purchases-section">
                    <div class="purchases-header">
                        <h2>Ваши покупки</h2>
                    </div>
                    <table class="purchases-table">
                        <thead>
                            <tr>
                                <th>Покупка</th>
                                <th>Дата</th>
                                <th>Статус</th>
                                <th>Рекомендация</th>
                            </tr>
                        </thead>
                        <tbody id="purchasesBody">
                            <!-- Данные будут заполняться динамически из базы -->
                        </tbody>
                    </table>
                        <div class="loading-spinner" id="loadingSpinner"><div class="spinner-border text-primary" role="status"></div></div>
                </div>
            }
            else
            {          
                <div class="text-center">
                    <h3>Войдите, чтобы просмотреть покупки</h3>
                    <p>Пожалуйста, <a href="/Identity/Account/Login" class="text-grey">войдите в аккаунт</a> или <a href="/Identity/Account/Register" class="text-grey">зарегистрируйтесь</a>.</p>
                </div>
            }
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

@if (User.Identity != null && User.Identity.IsAuthenticated)    {
        <script src="/js/purchases.js"></script>
    }
</body>
</html>